- name: Create a Partition, VLAN, Route domain and ip address on F5 BigIP lb
  hosts: all[0]
  connection: local

  tasks:
    - include: ~/f5-citrix/tasks/pri.yml

- name: Ha configuration
  hosts: hasec
  connection: local

  tasks:
    - include: ~/f5-citrix/tasks/sec.yml

